orders_status_invoice_id int default(0) not_null comment(Primary key - unique identifier for each invoice status)
language_id int default(1) not_null comment(FK to languages table - language for status name)
orders_status_invoice_name varchar(32) not_null comment(Invoice status name - e.g. Not Invoiced, Invoiced, Paid, Overdue)
--
primary orders_status_invoice_id language_id
idx_orders_status_invoice_name orders_status_invoice_name
##
engine innodb
character_set utf8mb4
collate utf8mb4_unicode_ci